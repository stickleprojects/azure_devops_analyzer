# Test environment overrides
# This file overrides .env/.env.resolved for testing

# For tests running on host machine (not in Docker):
# Connect to PostgreSQL container via localhost:5432
POSTGRES_HOST=localhost

# For tests running inside Docker:
# Use the Docker network hostname
# POSTGRES_HOST=timescaledb
